# Kontur.Talk Private Inbox

Неофициальный локальный прототип для преподавателя: собирает личные сообщения
участников встречи Контур.Толка в одно отдельное окно.

Ученики продолжают писать преподавателю обычные личные сообщения внутри Толка и
не видят ответы друг друга. Приложение на компьютере преподавателя последовательно
открывает диалоги с новыми сообщениями и показывает их в единой ленте с именами
отправителей.

## Важно

- Работает только в Windows с десктопным приложением Контур.Толка.
- Использует Windows UI Automation, а не официальный API Толка.
- Не запрашивает логин, пароль, токены или ключи и не перехватывает сетевой трафик.
- Открытые приложением сообщения становятся прочитанными в Толке.
- Интерфейс Толка может измениться и сломать прототип.
- Локальный лог содержит тексты сообщений. Он сохраняется в
  `.tmp/talk_private_messages.jsonl`; каталог `.tmp` исключён из Git.

## Установка

Нужны Python 3.11+ и [uv](https://docs.astral.sh/uv/).

```powershell
git clone https://github.com/AlexeyMalyshkinMIPT/kontur_talk.git
cd kontur_talk
uv sync
```

## Запуск

1. Запустите встречу в десктопном приложении Контур.Толка.
2. Откройте чат. Для более стабильной работы рекомендуется кнопка
   «В отдельном окне».
3. Запустите:

```powershell
.\start_talk_inbox.cmd --organizer "Имя преподавателя в Толке"
```

Или напрямую:

```powershell
uv run python talk_private_chat_window.py --organizer "Имя преподавателя в Толке"
```

Параметр `--organizer` скрывает исходящие сообщения преподавателя из общей ленты.
Если его не указать, приложение покажет все доступные сообщения.

## Консольный режим

Один проход по личным чатам:

```powershell
uv run python talk_private_message_reader.py --once --include-read
```

Постоянное наблюдение:

```powershell
uv run python talk_private_message_reader.py --watch --organizer "Имя преподавателя"
```

## Как это работает

Приложение находит доступное UIA-дерево встречи или отдельного окна `ktalk`,
открывает список «Личные» и обрабатывает диалоги со счётчиком новых сообщений.
При старте оно может прочитать уже существующие диалоги. Повторы подавляются по
диалогу, отправителю, отображаемому времени и тексту сообщения.

Это MVP для проверки сценария. Он не является официальным продуктом СКБ Контур.
